{
  "name": "banking-sync",
  "version": "1.0.0",
  "description": "Automated banking CSV sync via Playwright",
  "main": "sync.js",
  "scripts": {
    "sync": "bun sync.js",
    "test": "bun sync.js --test",
    "upload": "bun upload.js",
    "server": "bun server.js"
  },
  "dependencies": {
    "playwright": "^1.41.0"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.1"
  }
}
